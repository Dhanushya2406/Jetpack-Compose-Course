<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TrailProject"
        tools:targetApi="31">

        <activity android:name=".Quadrant"/>

        <activity android:name=".MainActivity"/>

        <activity android:name=".Trail"/>

        <activity android:name=".TaskCompleted"/>

        <activity android:name=".LazyColumnAndRow"/>

        <activity android:name=".ConstraintView"/>

        <activity android:name=".BottomNavBar"/>

        <activity android:name=".trailapi.UsingKtor"/>

        <activity android:name=".trailapi.AnotherApiUsingKtor"/>

        <activity android:name=".oldmethodusingretrofit.UsingRetrofit"/>

        <activity android:name=".DiceRoll"/>

        <activity android:name=".memoryleaks.MainActivity1"/>

        <activity android:name=".memoryleaks.SecondActivity"/>

        <activity android:name=".TipCalculator"/>


        <activity android:name=".scrollablelist.AffirmationListItem" />

        <activity android:name=".woof.Woof"/>

        <activity android:name=".superheros.SuperHeros"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.TrailProject">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>


    </application>

</manifest>